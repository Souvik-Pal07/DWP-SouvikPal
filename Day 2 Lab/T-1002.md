Triage Summary:
Summary: Finance user cannot open a shared mailbox after migration.
Impact: 1 user affected, finance workflow interruption, moderate to high business impact.
Known facts: user is in Finance; shared mailbox access fails; issue started after migration.
Missing info: exact error text, Outlook desktop vs web behavior, mailbox permission status, whether other delegated users are affected, and whether profile rebuild was attempted.
Likely category: migration-related mailbox permission or profile mapping issue.
First step: verify shared mailbox permissions and test access in web mail to isolate client vs service.

End-user comms:
Hi - we have resolved the shared mailbox access issue that appeared after migration. Your mailbox data is safe, and your normal email access is not at risk. Please close and reopen Outlook once. If the shared mailbox still does not appear, sign out and sign back in, then contact us and we will assist immediately.

Known-error record:
Symptom: User cannot open a shared mailbox after migration.
Cause: Shared mailbox permission and/or client profile mapping did not apply correctly during migration (to-verify).
Scope: Users moved in the same migration wave who rely on shared mailboxes (to-verify).
Workaround: Access the shared mailbox using web mail while client access is corrected.
Permanent fix: Reconcile shared mailbox permissions and refresh mailbox mapping in the user profile.

Closure note:
Resolved. Cause: post-migration shared mailbox permission/profile mapping issue (to-verify). Action: access path corrected and shared mailbox availability revalidated (to-verify). Preventive: include delegated mailbox validation in post-migration checks. User confirmed access (to-verify).
