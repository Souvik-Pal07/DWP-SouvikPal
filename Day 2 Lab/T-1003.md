Triage Summary:
Summary: AVD session disconnects at around 10 minutes, then reconnects.
Impact: at least 1 user affected, repeated session interruption, productivity degradation.
Known facts: AVD environment; disconnect occurs near 10 minutes; session reconnects afterward.
Missing info: user count affected, host pool details, network path quality, exact disconnect message, time pattern, and whether idle/active use changes behavior.
Likely category: session timeout, network path instability, or host policy issue.
First step: review AVD connection diagnostics and host/session timeout policy settings.

End-user comms:
Hi - we have stabilized your AVD connection issue that was dropping around the 10-minute mark. Your files and access are safe, and no data loss is expected from this incident. Please use your normal sign-in flow and let us know if you see another disconnect so we can check it immediately.

Known-error record:
Symptom: AVD session disconnects after about 10 minutes and then reconnects.
Cause: Session timeout policy, network interruption, or host configuration issue (to-verify).
Scope: Affected AVD users on the same host pool or network path (to-verify).
Workaround: Reconnect session when dropped; prefer stable wired connectivity where available.
Permanent fix: Correct timeout/policy settings and remediate host or network instability based on diagnostics.

Closure note:
Resolved. Cause: AVD session stability issue around timeout/network path (to-verify). Action: session policy/connection path corrected and repeat disconnect checks completed (to-verify). Preventive: add proactive monitoring for repeated short-interval reconnect patterns. User confirmed working (to-verify).
