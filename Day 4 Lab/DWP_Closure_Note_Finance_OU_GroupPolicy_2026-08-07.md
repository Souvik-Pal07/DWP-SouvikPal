Resolved. Cause: DHCP scope Option 006 on the affected Floor 3/Finance subnet still assigned decommissioned DNS server entries after migration, causing domain controller resolution failure and Group Policy processing failure on 3 of 4 Finance OU machines. Action: Updated DHCP Option 006 to current DNS (10.10.0.10 and approved set), removed decommissioned DNS entries, renewed leases on affected hosts, flushed DNS, and re-ran Group Policy processing; recovery verified and issue resolved at 09:09 AM. Preventive: Enforce a migration gate requiring DHCP DNS scope validation before DNS decommission, plus recurring scope audits and standard post-change smoke tests (lease, DC FQDN lookup, SYSVOL access, gpupdate). User confirmed working.
